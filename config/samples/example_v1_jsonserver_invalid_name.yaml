# Invalid example - Name does not follow convention (should be rejected by webhook)
apiVersion: example.com/v1
kind: JsonServer
metadata:
  name: my-server  # This should be rejected - must start with "app-"
  namespace: default
spec:
  replicas: 1
  jsonConfig: |
    {
      "data": []
    }
